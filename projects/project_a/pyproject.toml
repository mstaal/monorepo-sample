[project]
name = "project-a"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.3,<0.140.0",
    "lib-x",
    "pydantic>=2.11",
    "pydantic-settings<3.0.0,>=2.2.1",
]

[tool.uv.sources]
lib-x = { path = "../../libs/lib_x", editable = false }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = ["main.py", "endpoints"]
